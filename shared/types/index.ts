/**
 * Shared Types Index
 * ==================
 * Re-exports all shared types for easy importing.
 *
 * Usage:
 *   import { Generation, Element, ModelInfo } from '@/shared/types';
 *   // or
 *   import type { CreateGenerationRequest } from '../../../shared/types';
 */

export * from './api';
